# docker/.env — local Zabbix dev stack secrets
# Copy this file to docker/.env and fill in values.
# docker/.env is gitignored — NEVER commit it.

# PostgreSQL credentials
POSTGRES_DB=zabbix
POSTGRES_USER=zabbix
POSTGRES_PASSWORD=changeme

# Web frontend timezone
PHP_TZ=UTC
